export class PurchaseDetail {
  id: number;
  productName: string;
  productBrand: string;
  productModel: string;
  color: string;
  size: string;
  purchasePrice: number;
  requestedQuantity: number;
  receivedQuantity: number;
  subtotal: number;
  active: boolean;
}
